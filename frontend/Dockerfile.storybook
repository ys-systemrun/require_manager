# Runs the Storybook dev server for reviewing frontend components in isolation.
FROM node:22-alpine
WORKDIR /app

# Install dependencies first for better layer caching.
COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 6006
CMD ["npm", "run", "storybook"]
